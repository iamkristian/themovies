 <% flash.each do |key, value| %>
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <div class="alert alert-<%= key %>" role="alert"><%= value %></div>
      </div>
    </div>
  </div>
<% end -%>
